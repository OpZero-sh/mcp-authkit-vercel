# Turso Database Configuration
# Create a database at https://turso.tech
TURSO_DATABASE_URL=libsql://your-db-name.turso.io
TURSO_AUTH_TOKEN=your-auth-token-here

# Admin API Key (generate a strong random string)
# Used to authenticate /api/auth/api/servers endpoint
ADMIN_KEY=your-strong-random-admin-key

# Password Salt (generate a strong random string)
SALT=your-random-salt-string
